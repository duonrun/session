{
	"name": "duon/session",
	"description": "Duon session library",
	"type": "library",
	"license": "MIT",
	"homepage": "https://duon.run/session",
	"keywords": [
		"sessions"
	],
	"authors": [
		{
			"name": "Ernst",
			"email": "ernst@duon.run"
		}
	],
	"autoload": {
		"psr-4": {
			"Duon\\Session\\": "src/"
		}
	},
	"autoload-dev": {
		"psr-4": {
			"Duon\\Session\\Tests\\": "tests"
		}
	},
	"require": {
		"php": "^8.5",
		"ext-random": "*"
	},
	"require-dev": {
		"duon/dev": "^2.5"
	},
	"scripts": {
		"test": "./vendor/bin/phpunit --testdox --no-coverage --colors=always",
		"types": "./vendor/bin/psalm --no-cache",
		"mdlint": "markdownlint-cli2 README.md CHANGELOG.md LICENSE.md",
		"coverage": [
			"./vendor/bin/phpunit --testdox --path-coverage --coverage-text --coverage-php=coverage/cover.php --colors=always",
			"./vendor/bin/coverlyzer coverage/cover.php"
		],
		"ci": [
			"composer coverage",
			"composer types",
			"composer mdlint"
		]
	}
}
